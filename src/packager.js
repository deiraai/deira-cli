/*
 * @author David Menger
 */
'use strict';

const glob = require('glob');
const path = require('path');
const fs = require('fs');

const OUR_DIR = '.deira';
const DEPLOY_DIR = 'deploy';

function ensureDirectoryExists (dirname) {
    fs.stat(dirname, (err, res) => {

    });
}

function ensureDeployDirectory (cwd, ) {

}

function packager () {

}

module.exports = packager;
